# EMI calculation using math module

import math

p = float(input("Enter principal amount: "))
r = float(input("Enter rate of interest: "))
t = int(input("Enter time in years: "))

interest = (p * r * t) / 100

print("Interest =", math.ceil(interest))
